@startuml CodeUnits-Overview
skinparam defaultFontName "DejaVu Sans"
title CodeUnits of MatCountrySelector

[MatCountrySelector]
note as MatCountrySelectorNote
  mat_country_selector is a flutter-widget which lets the user choose a country from a configurable list of countries.
end note
MatCountrySelector .. MatCountrySelectorNote


@enduml