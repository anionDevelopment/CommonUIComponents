# Release notes

## Changes

- non-code-update
- The codeunit-files now implement the codeunit-specification-version 3.0.0
- Removed the configuration of GitVersion, which is not used anymore
- generate the two readme example images (light and dark mode) from the chromium visual-regression-screenshots instead of a hand-picked, hand-cropped one
- documented the readme-reference-images exception as an openspec spec instead of a readme note
