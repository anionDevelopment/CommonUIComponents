# Release notes

## Changes

- The peer-dependencies now require the angular-versions which the using applications actually have (@angular/cdk and @angular/material in 19.2.1 instead of 19.2.0). With the previous versions an installation aborted with ERESOLVE.
