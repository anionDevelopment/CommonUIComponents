# Release notes

## Changes

- added technical overhead
- generate the two readme example images (component closed and open) from the chromium visual-regression-screenshots instead of a hand-picked, hand-cropped one
- documented the readme-reference-images exception as an openspec spec instead of a readme note
- added the initial visual-regression-baselines
