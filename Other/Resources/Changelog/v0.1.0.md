# Release notes

## Changes

- Initial release
- `NgxCultureSelectorComponent` lets the user choose a culture from a configurable list of cultures and reports the choice via the output `cultureSelected`
- The repository implements the common project structure and is buildable with `scbuildcodeunits`
