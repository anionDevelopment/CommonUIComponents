# Release notes

## Changes

- Initial release
- The repository implements the common project structure with one code-unit, `MatCountrySelector`, and is buildable with `scbuildcodeunits`
- The build-, linting-, testcase- and reference-scripts of the code-unit are in place, as is the definition of the container the visual-regression-tests run in
