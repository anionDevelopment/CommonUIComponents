## 1. Move the permission note out of the readme

- [x] 1.1 Remove the "permitted to modify files in `<repo>/Other`" note from `ReadMe.md` and verify no reference to it remains there
- [x] 1.2 Verify the same rule is now expressed as a requirement in `openspec/changes/document-visual-regression-image-exception/specs/quality-check/readme-reference-images/spec.md`

## 2. No code changes required

- [x] 2.1 Confirm `UpdateVisualRegressionBaselines.py` already implements exactly the behavior described in the spec (it does - this change only documents it)
