# Example

An application which shows nothing but the culture-selector of the package `mat_culture_selector`.

It is started with `flutter run` (or, from the root of the repository, with `task rd`, which starts it in a
browser).
