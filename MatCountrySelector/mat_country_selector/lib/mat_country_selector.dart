/// A flutter-widget which lets the user choose a country, shown with its flag and its english name.
///
/// See [MatCountrySelector] for the widget itself, [MatCountry] for one country and [allCountries] for the list of
/// all of them which the widget offers by default.
library;

export 'src/all_countries.dart';
export 'src/mat_country.dart';
export 'src/mat_country_selector.dart';
