# Example

An application which shows nothing but the country-selector of the package `mat_country_selector`.

It is started with `flutter run` (or, from the root of the repository, with `task rd`, which starts it in a
browser).
